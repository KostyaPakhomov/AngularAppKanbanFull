// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.
// import firebase from 'firebase';

export const environment = {
  production: false,
  config: {
    apiKey: 'AIzaSyAB0DrqMaHIHwxhJK7uylQKZ7MdF7vr1PA',
    authDomain: 'angularappkanban.firebaseapp.com',
    databaseURL: 'https://angularappkanban-default-rtdb.firebaseio.com',
    projectId: 'angularappkanban',
    storageBucket: 'angularappkanban.appspot.com',
    messagingSenderId: '300837178479',
    appId: '1:300837178479:web:c47d809df210428e8e119a',
    measurementId: 'G-KL37B5V91D'
    }
  };
// firebase.initializeApp(environment.config);
// firebase.analytics();

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
